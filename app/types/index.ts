export interface CardProps {
  title: string;
  id: number;
  rating: number;
  date?: string;
  poster: string;
}

export interface HeroProps {
  backdrop: string;
  title: string;
  description: string;
}
